/*
 * Unit OpenAPI specifications
 * An OpenAPI specifications for unit-sdk clients
 *
 * The version of the OpenAPI document: 0.4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package unit.java.sdk.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.fasterxml.jackson.annotation.JsonTypeName;
import unit.java.sdk.model.ThreadBusinessAccountPurpose;
import unit.java.sdk.model.ThreadIndividualAccountPurpose;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import unit.java.sdk.JSON;

@jakarta.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.9.0")
@JsonDeserialize(using = IndividualApplicationAllOfAttributesAccountPurpose.IndividualApplicationAllOfAttributesAccountPurposeDeserializer.class)
@JsonSerialize(using = IndividualApplicationAllOfAttributesAccountPurpose.IndividualApplicationAllOfAttributesAccountPurposeSerializer.class)
public class IndividualApplicationAllOfAttributesAccountPurpose extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(IndividualApplicationAllOfAttributesAccountPurpose.class.getName());

    public static class IndividualApplicationAllOfAttributesAccountPurposeSerializer extends StdSerializer<IndividualApplicationAllOfAttributesAccountPurpose> {
        public IndividualApplicationAllOfAttributesAccountPurposeSerializer(Class<IndividualApplicationAllOfAttributesAccountPurpose> t) {
            super(t);
        }

        public IndividualApplicationAllOfAttributesAccountPurposeSerializer() {
            this(null);
        }

        @Override
        public void serialize(IndividualApplicationAllOfAttributesAccountPurpose value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class IndividualApplicationAllOfAttributesAccountPurposeDeserializer extends StdDeserializer<IndividualApplicationAllOfAttributesAccountPurpose> {
        public IndividualApplicationAllOfAttributesAccountPurposeDeserializer() {
            this(IndividualApplicationAllOfAttributesAccountPurpose.class);
        }

        public IndividualApplicationAllOfAttributesAccountPurposeDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public IndividualApplicationAllOfAttributesAccountPurpose deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = jp.readValueAsTree();
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize ThreadBusinessAccountPurpose
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (ThreadBusinessAccountPurpose.class.equals(Integer.class) || ThreadBusinessAccountPurpose.class.equals(Long.class) || ThreadBusinessAccountPurpose.class.equals(Float.class) || ThreadBusinessAccountPurpose.class.equals(Double.class) || ThreadBusinessAccountPurpose.class.equals(Boolean.class) || ThreadBusinessAccountPurpose.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((ThreadBusinessAccountPurpose.class.equals(Integer.class) || ThreadBusinessAccountPurpose.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((ThreadBusinessAccountPurpose.class.equals(Float.class) || ThreadBusinessAccountPurpose.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (ThreadBusinessAccountPurpose.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (ThreadBusinessAccountPurpose.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(ThreadBusinessAccountPurpose.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'ThreadBusinessAccountPurpose'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'ThreadBusinessAccountPurpose'", e);
            }

            // deserialize ThreadIndividualAccountPurpose
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (ThreadIndividualAccountPurpose.class.equals(Integer.class) || ThreadIndividualAccountPurpose.class.equals(Long.class) || ThreadIndividualAccountPurpose.class.equals(Float.class) || ThreadIndividualAccountPurpose.class.equals(Double.class) || ThreadIndividualAccountPurpose.class.equals(Boolean.class) || ThreadIndividualAccountPurpose.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((ThreadIndividualAccountPurpose.class.equals(Integer.class) || ThreadIndividualAccountPurpose.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((ThreadIndividualAccountPurpose.class.equals(Float.class) || ThreadIndividualAccountPurpose.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (ThreadIndividualAccountPurpose.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (ThreadIndividualAccountPurpose.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(ThreadIndividualAccountPurpose.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'ThreadIndividualAccountPurpose'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'ThreadIndividualAccountPurpose'", e);
            }

            if (match == 1) {
                IndividualApplicationAllOfAttributesAccountPurpose ret = new IndividualApplicationAllOfAttributesAccountPurpose();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format("Failed deserialization for IndividualApplicationAllOfAttributesAccountPurpose: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public IndividualApplicationAllOfAttributesAccountPurpose getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "IndividualApplicationAllOfAttributesAccountPurpose cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public IndividualApplicationAllOfAttributesAccountPurpose() {
        super("oneOf", Boolean.FALSE);
    }

    public IndividualApplicationAllOfAttributesAccountPurpose(ThreadBusinessAccountPurpose o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public IndividualApplicationAllOfAttributesAccountPurpose(ThreadIndividualAccountPurpose o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("ThreadBusinessAccountPurpose", ThreadBusinessAccountPurpose.class);
        schemas.put("ThreadIndividualAccountPurpose", ThreadIndividualAccountPurpose.class);
        JSON.registerDescendants(IndividualApplicationAllOfAttributesAccountPurpose.class, Collections.unmodifiableMap(schemas));
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return IndividualApplicationAllOfAttributesAccountPurpose.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * ThreadBusinessAccountPurpose, ThreadIndividualAccountPurpose
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(ThreadBusinessAccountPurpose.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(ThreadIndividualAccountPurpose.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be ThreadBusinessAccountPurpose, ThreadIndividualAccountPurpose");
    }

    /**
     * Get the actual instance, which can be the following:
     * ThreadBusinessAccountPurpose, ThreadIndividualAccountPurpose
     *
     * @return The actual instance (ThreadBusinessAccountPurpose, ThreadIndividualAccountPurpose)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `ThreadBusinessAccountPurpose`. If the actual instance is not `ThreadBusinessAccountPurpose`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ThreadBusinessAccountPurpose`
     * @throws ClassCastException if the instance is not `ThreadBusinessAccountPurpose`
     */
    public ThreadBusinessAccountPurpose getThreadBusinessAccountPurpose() throws ClassCastException {
        return (ThreadBusinessAccountPurpose)super.getActualInstance();
    }

    /**
     * Get the actual instance of `ThreadIndividualAccountPurpose`. If the actual instance is not `ThreadIndividualAccountPurpose`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ThreadIndividualAccountPurpose`
     * @throws ClassCastException if the instance is not `ThreadIndividualAccountPurpose`
     */
    public ThreadIndividualAccountPurpose getThreadIndividualAccountPurpose() throws ClassCastException {
        return (ThreadIndividualAccountPurpose)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof ThreadIndividualAccountPurpose) {
        if (getActualInstance() != null) {
          joiner.add(String.format("%sone_of_0%s=%s", prefix, suffix, URLEncoder.encode(String.valueOf(getActualInstance()), StandardCharsets.UTF_8).replaceAll("\\+", "%20")));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof ThreadBusinessAccountPurpose) {
        if (getActualInstance() != null) {
          joiner.add(String.format("%sone_of_1%s=%s", prefix, suffix, URLEncoder.encode(String.valueOf(getActualInstance()), StandardCharsets.UTF_8).replaceAll("\\+", "%20")));
        }
        return joiner.toString();
    }
    return null;
  }

}

