/*
 * Unit OpenAPI specifications
 * An OpenAPI specifications for unit-sdk clients
 *
 * The version of the OpenAPI document: 0.4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package unit.java.sdk.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.fasterxml.jackson.annotation.JsonTypeName;
import unit.java.sdk.model.ThreadBusinessSourceOfFunds;
import unit.java.sdk.model.ThreadIndividualSourceOfFunds;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import unit.java.sdk.JSON;

@jakarta.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.9.0")
@JsonDeserialize(using = IndividualApplicationAllOfAttributesSourceOfFunds.IndividualApplicationAllOfAttributesSourceOfFundsDeserializer.class)
@JsonSerialize(using = IndividualApplicationAllOfAttributesSourceOfFunds.IndividualApplicationAllOfAttributesSourceOfFundsSerializer.class)
public class IndividualApplicationAllOfAttributesSourceOfFunds extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(IndividualApplicationAllOfAttributesSourceOfFunds.class.getName());

    public static class IndividualApplicationAllOfAttributesSourceOfFundsSerializer extends StdSerializer<IndividualApplicationAllOfAttributesSourceOfFunds> {
        public IndividualApplicationAllOfAttributesSourceOfFundsSerializer(Class<IndividualApplicationAllOfAttributesSourceOfFunds> t) {
            super(t);
        }

        public IndividualApplicationAllOfAttributesSourceOfFundsSerializer() {
            this(null);
        }

        @Override
        public void serialize(IndividualApplicationAllOfAttributesSourceOfFunds value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class IndividualApplicationAllOfAttributesSourceOfFundsDeserializer extends StdDeserializer<IndividualApplicationAllOfAttributesSourceOfFunds> {
        public IndividualApplicationAllOfAttributesSourceOfFundsDeserializer() {
            this(IndividualApplicationAllOfAttributesSourceOfFunds.class);
        }

        public IndividualApplicationAllOfAttributesSourceOfFundsDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public IndividualApplicationAllOfAttributesSourceOfFunds deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = jp.readValueAsTree();
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize ThreadBusinessSourceOfFunds
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (ThreadBusinessSourceOfFunds.class.equals(Integer.class) || ThreadBusinessSourceOfFunds.class.equals(Long.class) || ThreadBusinessSourceOfFunds.class.equals(Float.class) || ThreadBusinessSourceOfFunds.class.equals(Double.class) || ThreadBusinessSourceOfFunds.class.equals(Boolean.class) || ThreadBusinessSourceOfFunds.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((ThreadBusinessSourceOfFunds.class.equals(Integer.class) || ThreadBusinessSourceOfFunds.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((ThreadBusinessSourceOfFunds.class.equals(Float.class) || ThreadBusinessSourceOfFunds.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (ThreadBusinessSourceOfFunds.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (ThreadBusinessSourceOfFunds.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(ThreadBusinessSourceOfFunds.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'ThreadBusinessSourceOfFunds'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'ThreadBusinessSourceOfFunds'", e);
            }

            // deserialize ThreadIndividualSourceOfFunds
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (ThreadIndividualSourceOfFunds.class.equals(Integer.class) || ThreadIndividualSourceOfFunds.class.equals(Long.class) || ThreadIndividualSourceOfFunds.class.equals(Float.class) || ThreadIndividualSourceOfFunds.class.equals(Double.class) || ThreadIndividualSourceOfFunds.class.equals(Boolean.class) || ThreadIndividualSourceOfFunds.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((ThreadIndividualSourceOfFunds.class.equals(Integer.class) || ThreadIndividualSourceOfFunds.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((ThreadIndividualSourceOfFunds.class.equals(Float.class) || ThreadIndividualSourceOfFunds.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (ThreadIndividualSourceOfFunds.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (ThreadIndividualSourceOfFunds.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(ThreadIndividualSourceOfFunds.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'ThreadIndividualSourceOfFunds'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'ThreadIndividualSourceOfFunds'", e);
            }

            if (match == 1) {
                IndividualApplicationAllOfAttributesSourceOfFunds ret = new IndividualApplicationAllOfAttributesSourceOfFunds();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format("Failed deserialization for IndividualApplicationAllOfAttributesSourceOfFunds: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public IndividualApplicationAllOfAttributesSourceOfFunds getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "IndividualApplicationAllOfAttributesSourceOfFunds cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public IndividualApplicationAllOfAttributesSourceOfFunds() {
        super("oneOf", Boolean.FALSE);
    }

    public IndividualApplicationAllOfAttributesSourceOfFunds(ThreadBusinessSourceOfFunds o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public IndividualApplicationAllOfAttributesSourceOfFunds(ThreadIndividualSourceOfFunds o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("ThreadBusinessSourceOfFunds", ThreadBusinessSourceOfFunds.class);
        schemas.put("ThreadIndividualSourceOfFunds", ThreadIndividualSourceOfFunds.class);
        JSON.registerDescendants(IndividualApplicationAllOfAttributesSourceOfFunds.class, Collections.unmodifiableMap(schemas));
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return IndividualApplicationAllOfAttributesSourceOfFunds.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * ThreadBusinessSourceOfFunds, ThreadIndividualSourceOfFunds
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(ThreadBusinessSourceOfFunds.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(ThreadIndividualSourceOfFunds.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be ThreadBusinessSourceOfFunds, ThreadIndividualSourceOfFunds");
    }

    /**
     * Get the actual instance, which can be the following:
     * ThreadBusinessSourceOfFunds, ThreadIndividualSourceOfFunds
     *
     * @return The actual instance (ThreadBusinessSourceOfFunds, ThreadIndividualSourceOfFunds)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `ThreadBusinessSourceOfFunds`. If the actual instance is not `ThreadBusinessSourceOfFunds`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ThreadBusinessSourceOfFunds`
     * @throws ClassCastException if the instance is not `ThreadBusinessSourceOfFunds`
     */
    public ThreadBusinessSourceOfFunds getThreadBusinessSourceOfFunds() throws ClassCastException {
        return (ThreadBusinessSourceOfFunds)super.getActualInstance();
    }

    /**
     * Get the actual instance of `ThreadIndividualSourceOfFunds`. If the actual instance is not `ThreadIndividualSourceOfFunds`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ThreadIndividualSourceOfFunds`
     * @throws ClassCastException if the instance is not `ThreadIndividualSourceOfFunds`
     */
    public ThreadIndividualSourceOfFunds getThreadIndividualSourceOfFunds() throws ClassCastException {
        return (ThreadIndividualSourceOfFunds)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof ThreadIndividualSourceOfFunds) {
        if (getActualInstance() != null) {
          joiner.add(String.format("%sone_of_0%s=%s", prefix, suffix, URLEncoder.encode(String.valueOf(getActualInstance()), StandardCharsets.UTF_8).replaceAll("\\+", "%20")));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof ThreadBusinessSourceOfFunds) {
        if (getActualInstance() != null) {
          joiner.add(String.format("%sone_of_1%s=%s", prefix, suffix, URLEncoder.encode(String.valueOf(getActualInstance()), StandardCharsets.UTF_8).replaceAll("\\+", "%20")));
        }
        return joiner.toString();
    }
    return null;
  }

}

